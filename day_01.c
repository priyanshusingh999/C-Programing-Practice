#include <stdio.h>

void main() {
    int a=10,b=20, c=a+b;
    printf("Add of %d and %d is %d", a,b,c);
    int d=10,e=20, f=d-e;
    printf("Sub of %d and %d is %d", d,e,f);
    int g=10,h=20, i=g*h;
    printf("Add of %d and %d is %d", g,h,i);
    int j=10,k=20, l=j/k;
    printf("Add of %d and %d is %d", j,k,l);
}